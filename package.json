{
  "name": "boxconfiguration",
  "version": "1.0.0",
  "description": "box configurator",
  "main": "config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy":"gh-pages -d dist"
  },
  "author": "venkatesh",
  "license": "ISC",
  "dependencies": {
    "gh-pages": "^5.0.0"
  }
}
